# Supabase SQL Scripts

This directory contains **ops and verification scripts** that are run manually, not as part of the migration pipeline.

These scripts are for ad-hoc inspection, debugging, and data verification. They are not applied by `supabase db push` or `supabase migration up`.
